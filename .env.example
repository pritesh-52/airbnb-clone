# Root-level defaults. Copy to `.env` for values shared by both apps;
# per-app settings live in apps/api/.env.example and apps/web/.env.example.

# Ports each app binds to when run via `npm run dev`.
API_PORT=4000
WEB_PORT=3000
